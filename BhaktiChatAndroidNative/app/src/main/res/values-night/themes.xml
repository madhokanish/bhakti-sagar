<resources>
    <!-- Keep light theme even when the device is in dark mode. Status/nav bar transparency
         and icon color are set in code via enableEdgeToEdge() — see values/themes.xml. -->
    <style name="Theme.BhaktiChat" parent="Theme.Material3.Light.NoActionBar" />
</resources>
