<resources>
    <!-- Force light theme always — Compose controls all UI colours. Status/nav bar
         transparency and icon color are set in code via enableEdgeToEdge(), not here —
         the old android:statusBarColor/navigationBarColor/windowLightStatusBar attributes
         are deprecated as of Android 15 and were flagged by Play Console's pre-launch report. -->
    <style name="Theme.BhaktiChat" parent="Theme.Material3.Light.NoActionBar" />
</resources>
